<Window x:Class="Projet_Gestion_Finance.Models.FormProjets"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:local="clr-namespace:Projet_Gestion_Finance.Models"
        mc:Ignorable="d"
        Title="Mes Projets" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <!-- ScrollViewer pour activer le défilement -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- En-tête -->

                <!-- Filtres -->
                <StackPanel Width="845" Height="68" Background="#B22C3E50">
                    <TextBlock Text="Mes Projets" HorizontalAlignment="Left" FontSize="20" FontWeight="Bold" Margin="10,20,0,-28"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,85,0" MouseDown="StackPanel_MouseDown">
                        <TextBlock Text="Accueil" FontSize="20" FontWeight="Bold" Margin="0,5,10,0" />
                        <Image Source="/Ressource/Icons/home.png" Width="35" Height="35" HorizontalAlignment="Right" Margin="0,0,5,0" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Margin="20,15,10,0" Orientation="Horizontal" HorizontalAlignment="Left" Width="500">
                    <TextBlock Text="Etat prévu des projets pour le " Width="175" Height="25" Margin="5,10" Foreground="Black" FontWeight="Bold" />
                    <DatePicker x:Name="dtpDate" Width="100" Height="27" Margin="3,10" Padding="2"/>
                    <Button x:Name="btnRefresh" Margin="10" BorderThickness="0" Height="26" Width="29">
                        <Image Source="/Ressource/refresh.png" Width="56" Height="25" RenderTransformOrigin="0.777,0.5" MouseLeftButtonDown="btnRefresh_Click"/>
                    </Button>
                </StackPanel>

                <!-- Liste des projets -->
                <GroupBox Header="Liste des projets" FontSize="14">
                    <StackPanel>
                        <ListView Background="AliceBlue" x:Name="lstProjets" Width="740" Height="200" Margin="0,0,66,0">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="NOM" DisplayMemberBinding="{Binding Nom}" Width="150"/>
                                    <GridViewColumn Header="Avancement" Width="150">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Avancement, StringFormat=\{0:C2\}}" Foreground="Green" TextAlignment="Right" />
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Objectif" Width="150">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                    <TextBlock Text="{Binding Objectif, StringFormat=\{0:C2\}}">
                                                        <TextBlock.Style>
                                                            <Style TargetType="{x:Type TextBlock}">
                                                                <Setter Property="Foreground" Value="Red"/>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Reste" Width="150">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Reste, StringFormat=\{0:C2\}}" Foreground="Green" TextAlignment="Right" />
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Progression" Width="150">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <ProgressBar Value="{Binding Progression}" Maximum="100" Minimum="0" Width="150" Height="20"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>

                        <!-- Boutons d'action -->
                        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Margin="0,10">
                            <Button x:Name="btnCreerCategorie" Content="Creer" Width="100" Height="25" Click="btnCreerCategorie_Click"/>
                            <Button x:Name="btnModifierCategorie" Content="Modifier" Width="100" Height="25" Margin="10,0,0,0" Click="btnModifierCategorie_Click"/>
                            <Button x:Name="btnSupprimerCategorie" Content="Supprimer" Width="100" Height="25" Margin="10,0,0,0" Click="btnSupprimerCategorie_Click"/>
                        </StackPanel>

                        <Label Foreground="Black" FontSize="14" Margin="40,5,10,0" FontWeight="Bold" Content="Statistiques" HorizontalAlignment="Left"/>
                        <!-- Graphique -->
                        <lvc:CartesianChart x:Name="graphique" Height="300" Width="700" Margin="0,20,0,0">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Title="Catégories" x:Name="Labels"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Montant ($)"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>